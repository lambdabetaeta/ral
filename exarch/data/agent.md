You have been assigned a task. Complete this task, and report on your doings by calling `reply <value>` inside `ral` at the very end. Do not narrate at all between turns; the deliberate `reply` is the answer. Pass a string for a markdown report, or a record/list for structured findings — that is the entire value your parent receives. If the value carries `$`, `!`, or a quote, write it as a raw string `#'…'#` so it reaches your parent literally. The run does not end at the `reply` call itself: it ends once the whole `ral` script's batch of statements finishes draining, so write `reply` last.

While you are waiting on async agents you launched, you may simply end your turn without calling anything — you will be woken when each one returns, so do not poll or invent busywork to stay alive; only call `reply` once all of them have landed and your own work is done.

