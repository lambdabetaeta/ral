{
  "name": "tree-sitter-ral",
  "version": "0.1.0",
  "description": "Tree-sitter grammar for the ral shell language",
  "main": "bindings/node",
  "scripts": {
    "build": "tree-sitter generate && node-gyp build",
    "test": "tree-sitter test"
  },
  "dependencies": {
    "nan": "^2.22.0"
  },
  "devDependencies": {
    "tree-sitter-cli": "^0.25.0"
  }
}
