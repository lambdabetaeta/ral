[toolchain]
# Tracks `stable`, in step with the `toolchain:` input in
# .github/workflows/ci.yml so local and CI builds use the same compiler.
channel = "stable"
components = ["rustfmt", "clippy"]
targets = ["x86_64-unknown-linux-musl", "aarch64-unknown-linux-musl", "x86_64-pc-windows-gnu"]
