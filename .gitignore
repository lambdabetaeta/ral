target/
target-host/
.target-linux/
.build-cache/
.worktrees/

# Private dev-notes checkout (lambdabetaeta/ral-dev). Kept alongside the source
# for convenience; deliberately not part of the public repository, so it is
# ignored outright rather than declared as a submodule no clone could fetch.
dev/

# qmd project-local search index (generated; rebuild with scripts/wiki-index.sh)
.qmd/

# tree-sitter-ral build artefacts
editors/tree-sitter-ral/node_modules/
editors/tree-sitter-ral/build/
editors/tree-sitter-ral/bindings/
editors/tree-sitter-ral/.ts-lib/
editors/tree-sitter-ral/package-lock.json
editors/tree-sitter-ral/*.wasm
editors/tree-sitter-ral/*.so
editors/tree-sitter-ral/*.obj
editors/tree-sitter-ral/*.lib
editors/tree-sitter-ral/*.exp

*.swp
*.swo
*~
# generated site pages (scripts/render-site.py)
site/index.html
site/examples.html
site/tutorial.html
site/spec.html
site/rationale.html
site/exarch/index.html
site/exarch/profiles.html
site/synod/index.html
# generated wiki sub-site (scripts/wiki/build.sh — Quartz v5)
site/wiki/

CLAUDE.md

.letta

# cargo-fuzz builds a separate crate; its lock and target dir are generated,
# and the fuzz target is a tool for a person to run, not part of the build.
guest-net/fuzz/Cargo.lock
guest-net/fuzz/target/

# Working notes for a person, not part of the tree.
scratch/
.scratch/

# Tauri regenerates a permission schema per host it is built on. The three
# synod ships on are tracked; a Linux host's is a build product of a platform
# synod has no bundle for.
synod/gen/schemas/linux-schema.json
